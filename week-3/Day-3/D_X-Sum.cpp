#include<bits/stdc++.h>
using namespace std;
int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    cout.tie(NULL);
    long long  t;
    while(t--)
    {
        long long n,m;
        cin>>n>>m;
        long long ar[n][m];
        for(long long i=0;i<n;i++)
        {
            for(long long j=0;j<m;j++)
            {
                cin>>ar[n][m];
            }
        }
        long long ans=INT_MIN;
        for(long long i=0;i<n;i++)
        {
            for(long long j=0;j<m;j++)
            {

            }
        }

    }

}
